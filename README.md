# AIC8800 Linux Driver

AIC8800 WiFi 芯片 Linux 内核驱动程序，针对 Arch Linux 和最新内核版本进行了优化和修复。

## 项目概述

本项目基于绿联官方 AIC8800 驱动源码，在 [sqlwwx/aic8800](https://github.com/sqlwwx/aic8800) 的适配工作基础上，针对最新的 Linux 内核版本进行了深度改进和功能扩展。

### 主要改进与新增功能

相比原始驱动，本 Fork 版本包含以下重要改进：

1. **内核兼容性修复**
   - 修复了 Linux Kernel 6.17.x 系列的编译错误
   - 适配了内核 API 变更，确保在最新内核上正常工作
   - 已测试支持：Linux 6.17.1-arch1-1 至 6.17.7-arch1-1

2. **WLAN 接口编号问题修复**
   - 解决了网络接口命名和编号不一致的问题
   - 改善了多网卡环境下的设备识别

3. **扩展芯片支持**
   - 新增 AIC8800-88DE 芯片变体支持
   - 完善了对多种 AIC8800 系列芯片的兼容性

4. **监控模式支持**
   - 添加了 Monitor Mode（监控模式）功能
   - 支持无线网络数据包捕获和分析
   - 适用于网络安全测试和调试场景

5. **构建系统优化**
   - 并行编译支持（使用 `-j$(nproc)`）
   - 改进的 Makefile 配置

## 硬件支持

### 支持的芯片型号

- **AIC8800D80** - 主要支持芯片
- **AIC8800DC**
- **AIC8800DCDW**
- **AIC8800-88DE** - 新增支持

### 已测试硬件

- **绿联 UGREEN AX300-CM762** USB 无线网卡 ✅

理论上支持所有基于 AIC8800 系列芯片的 USB WiFi 适配器。

## 系统支持

### 操作系统

- **Arch Linux** (主要测试平台)
- 其他基于 Linux 的发行版（需要相应内核版本）

### 内核版本

- **Linux Kernel 6.17.1-arch1-1** ✅ 已测试
- **Linux Kernel 6.17.7-arch1-1** ✅ 已测试
- **Linux Kernel 6.17.x 系列** 🟢 应该可以正常工作

> **注意**: 其他内核版本可能需要额外的适配工作。

### 系统架构

- **x86_64** (x64) ✅ 已测试

## 功能特性

- ✅ 2.4GHz 和 5GHz 双频支持 (802.11ac)
- ✅ USB 传输接口
- ✅ Station 模式（客户端模式）
- ✅ AP 模式（热点模式）
- ✅ Monitor 模式（监控模式）
- ✅ WiFi + Bluetooth 共存
- ✅ 多通道并发（MCC）
- ✅ 电源管理

## 编译安装

### 前置要求

```bash
# 确保已安装内核头文件
sudo pacman -S linux-headers

# 确保已安装必要的编译工具
sudo pacman -S base-devel
```

### 安装步骤

```bash
# 1. 克隆仓库
git clone https://github.com/BLUEMOON233/AIC8800-Linux-Driver.git
cd AIC8800-Linux-Driver

# 2. 安装固件和 udev 规则（首次安装必需）
sudo sh install_setup.sh

# 3. 编译驱动
cd drivers/aic8800
make

# 4. 安装驱动模块
sudo make install

# 5. 加载驱动
sudo modprobe cfg80211
sudo modprobe aic_load_fw
sudo modprobe aic8800_fdrv

# 6. 验证驱动加载
lsmod | grep aic
```

### 卸载驱动

```bash
# 卸载内核模块
sudo make uninstall

# 清理编译文件
make clean

# 移除固件和 udev 规则
sudo sh uninstall_setup.sh
```

## 使用说明

### 基本使用

驱动加载后，系统会自动识别网卡并创建网络接口（通常为 `wlan0` 或 `wlp*`）。

```bash
# 查看网络接口
ip link show

# 使用 NetworkManager 连接 WiFi
nmcli device wifi list
nmcli device wifi connect <SSID> password <密码>
```

### 监控模式使用

```bash
# 切换到监控模式
sudo ip link set wlan0 down
sudo iw dev wlan0 set type monitor
sudo ip link set wlan0 up

# 使用工具进行抓包（如 airodump-ng）
sudo airodump-ng wlan0

# 恢复为管理模式
sudo ip link set wlan0 down
sudo iw dev wlan0 set type managed
sudo ip link set wlan0 up
```

## 故障排除

### 驱动加载失败

```bash
# 查看内核日志
dmesg | grep aic

# 检查固件文件
ls -la /lib/firmware/aic8800D80/
```

### 固件加载错误

确保 `install_setup.sh` 已正确执行，固件文件应位于 `/lib/firmware/aic8800D80/`。

### 网卡未识别

```bash
# 检查 USB 设备
lsusb | grep -i aic

# 重新加载驱动
sudo modprobe -r aic8800_fdrv aic_load_fw
sudo modprobe aic_load_fw
sudo modprobe aic8800_fdrv
```

## 项目结构

```
AIC8800-Linux-Driver/
├── drivers/aic8800/          # 驱动源码
│   ├── aic_load_fw/          # 固件加载模块
│   └── aic8800_fdrv/         # 主驱动模块
├── fw/aic8800D80/            # 固件文件
├── install_setup.sh          # 安装脚本
├── uninstall_setup.sh        # 卸载脚本
└── README.md                 # 本文件
```

## 贡献与反馈

欢迎提交 Issue 和 Pull Request！

如遇到问题，请提供以下信息：
- 操作系统和内核版本 (`uname -r`)
- 硬件型号
- 相关日志输出 (`dmesg | grep aic`)

## 致谢

本项目的开发和维护得益于以下资源和项目：

- [绿联官方 AX300 驱动](https://www.ugreen.com/) - 原始驱动源码
- [sqlwwx/aic8800](https://github.com/sqlwwx/aic8800) - Linux 适配工作
- AI 辅助工具（Codex、Claude）用于代码分析和修改

特别感谢开源社区的贡献者们！

## 许可证

本项目继承原始驱动的许可证。详情请参阅相关源码文件头部的许可证声明。

## 更新日志

### 2025.11.11
- ✅ 确认在 Linux Kernel 6.17.7-arch1-1 (Arch Linux x64) 上编译通过
- ✅ 添加 AIC8800-88DE 芯片支持
- ✅ 修复 WLAN 接口编号问题
- ✅ 添加监控模式功能

### 首次发布
- ✅ 修复 AIC8800 驱动在 Linux Kernel 6.17.1-arch1-1 的编译错误
- ✅ 基础功能验证通过
